import React from 'react';

const UseReducerCounter = () => {
  return (
    <div>
      <h2>useReducer Complex Counter</h2>
      <p>Implement a counter with complex state (count, step, min, max) using useReducer.</p>
    </div>
  );
};

export default UseReducerCounter;

