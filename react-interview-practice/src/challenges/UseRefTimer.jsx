import React from 'react';

const UseRefTimer = () => {
  return (
    <div>
      <h2>useRef Timer & DOM Access</h2>
      <p>Use useRef for mutable variables (timer ID) and direct DOM manipulation (focus).</p>
    </div>
  );
};

export default UseRefTimer;

