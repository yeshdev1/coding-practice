import React from 'react';

const CountdownTimer = () => {
  return (
    <div>
      <h2>Countdown Timer with Pause/Resume</h2>
      <p>Build a countdown timer that can be paused and resumed without drifting, handling the interval cleanup and restart logic precisely.</p>
    </div>
  );
};

export default CountdownTimer;

