import React from 'react';

const Tabs = () => {
  return (
    <div>
      <h2>Tabs</h2>
      <p>Create a reusable Tabs component that renders different content based on selection.</p>
    </div>
  );
};

export default Tabs;

